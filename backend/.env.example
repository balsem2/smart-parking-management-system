DATABASE_URL=postgresql+psycopg://smartpark:smartpark_password@localhost:5433/smartpark
FRONTEND_URL=http://127.0.0.1:5173

# SMTP settings used to send staff password setup links
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=no-reply@example.com
SMTP_USE_TLS=true
